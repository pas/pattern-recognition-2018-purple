# Ubuntu

## Prerequisites
Install Tkinter
sudo apt-get install python-tk

## start virtual enc

Goto level where virtualenv was installed (in my case tensorflow)
    source bin/activate"

## deactivate virtualenc
    deactivate

## to install a requirements
    pip install -r requirements.txt

